export type Routes = {
  auth: {
    root: string,
    title: string,
  },
  create: {
    root: string,
    title: string,
  },
  links: {
    root: string,
    title: string,
  },
  details: {
    root: string,
    title: string,
  },
};
